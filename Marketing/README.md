# Opus Marketing

Marketing site for the Opus design system and `opus-react` component library.

## Development

```bash
npm install
npm run dev
```

Runs on [http://localhost:3003](http://localhost:3003) by default so it does not clash with other dev servers in this repo.

## Build

```bash
npm run build
npm start
```
