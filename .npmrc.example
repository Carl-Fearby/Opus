# Copy to .npmrc in the repo root (gitignored) or export NPM_TOKEN before running deploy.sh.
# Generate a granular "Publish" token at https://www.npmjs.com/settings/~/tokens
//
registry.npmjs.org/:_authToken=${NPM_TOKEN}
